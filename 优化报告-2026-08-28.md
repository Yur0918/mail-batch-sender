# 优化报告 —— 邮件批量发送工具（2026-08-28 两轮迭代）

本次迭代以"经办每天实际用它发监管件/工单邮件"的使用者视角出发，先梳理痛点清单，再分两轮实现并逐项验证。所有改动保持既有兼容边界：**Win7 + Node 12 + Chrome 109 / Firefox 115 ESR 可用，零构建、零新增依赖**。

## 一、使用者视角的需求分析

站在经办的位置过一遍日常流程（上传 → 核对 → 发送 → 记录），发现的痛点：

| # | 痛点 | 影响 | 轮次 |
|---|------|------|------|
| 1 | 发送是黑盒：点确认后只有"发送中…"，50 封按 3 秒间隔要两分半，看不到进度、失败数，不能取消 | 发送期间只能干等，出问题无法止损 | R1 |
| 2 | 结果只有一条提示"成功 X 失败 Y"，失败的哪封、为什么，要去日志翻 CSV | 失败补发麻烦、易漏 | R1 |
| 3 | 正文写错一个字，只能改本地文件重新上传 | 修正成本高 | R1 |
| 4 | 发送前看不到渲染后的邮件效果，只有 36 字摘要 | 心里没底，发出去才发现格式错 | R1 |
| 5 | "手动添加"用两个原生 prompt() 弹窗 | 体验差、易误关 | R1 |
| 6 | 同一封邮件当天可能误发两遍（重复发监管件是事故） | 合规风险 | R1 |
| 7 | 发送间隔写死 3 秒 | 量大太慢、紧急太磨叽 | R1 |
| 8 | 发送日志只能全量倒序肉眼看 | 找失败记录费劲 | R2 |
| 9 | 日志 CSV 在目录里，界面没有下载入口 | 交给主管/留档要翻文件夹 | R2 |
| 10 | 模板变量 {平台名称}{客户姓名}{日期}… 靠记忆手打 | 打错就校验失败 | R2 |
| 11 | 打开页面不知道"今天发了多少、账号是否就绪" | 缺乏一眼安心感 | R2 |
| 12 | 历史失败批次的上传临时文件残留 | 磁盘垃圾慢慢累积 | R2 |

## 二、第一轮实现（发送体验）

1. **发送进度实时化**：`/api/send-draft` 改为后台批次模式，立即返回 `batchId`；新增 `GET /api/send-progress`（逐封状态）与 `POST /api/send-cancel`。前端"④ 发送进度"卡片：进度条、成功/失败计数、逐封清单实时滚动、当前正在发送的主题。
2. **中途取消**：取消请求后"当前这封发完即停"，剩余条目标记"已取消"，**未尝试的条目不写发送日志**。
3. **结果报告与一键重发**：发送结束逐条回写列表状态（已发送/发送失败/已取消），失败与取消的条目保留在列表，可修正后点"仅重发失败项"；配合按钮"清空列表"。
4. **行内编辑正文**：每行"编"按钮打开编辑弹窗，Markdown 实时预览，保存后以编辑内容为准（不再依赖上传文件）。
5. **整封邮件预览**：每行"预览"按钮：主题/收件人/发件账号/附件 + 正文 HTML 渲染效果，与收件人实际看到的一致。
6. **手动添加表单化**：两个 prompt() 换成正经弹窗（平台下拉/客户/类型/正文 + 实时预览）。
7. **重复发送提醒**：校验时对照当日发送日志，同"平台+客户+主题"且当日已成功发过 → 黄色警告（不阻断），表格行内出现黄色提示行。校验警告（如"平台未配附件"）同样以黄色呈现，与红色错误明确区分。
8. **发送间隔可选**：确认发送处可选 2/3/5/8/10 秒每封。
9. **并发守卫**：一个批次进行中再发起会被拒绝，防止双开误发。

### R1 过程中修复的既有缺陷

- **正文优先级矛盾**：`lib/draft.js` 注释称手动正文"高于模板"，代码却先套模板——编辑正文保存后会被模板覆盖。已改为 上传文件 > 手动正文 > 模板兜底。
- **失败后临时文件被误删**：原实现整批结束后统一清理上传文件，失败条目重发时正文/附件已丢。现改为**只清理已成功发送条目**的文件。

## 三、第二轮实现（效率与安心感）

1. **日志筛选/搜索/统计**：发送日志页新增状态筛选（成功/失败）、平台筛选、关键词搜索（客户/主题/收件邮箱）、今日发送统计条（已发/成功/失败）、"导出 CSV"按钮（带 BOM，Excel 打开不乱码）。
2. **模板变量点选插入**：模板编辑弹窗显示 9 个常用变量（{平台名称} {客户姓名} {日期} {昨日} 等），点击插入到最近聚焦的主题/正文输入框光标处。
3. **上传页总览条**：今日已发 N 封（成功/失败）、平台/类型规模、启用账号一览，页面打开与发送结束后自动刷新。
4. **页面刷新后进度恢复**：后台批次不因关页/刷新中断；重新打开页面自动恢复"进行中批次"的进度显示。
5. **拒发精确定位**：校验拒发时后端返回出错条目的序号，前端精确标注到行（原来按平台+主题猜对位，同平台同主题会标错）。
6. **手动添加实时预览**、**favicon 404 噪音消除**、**启动时自动清理 24 小时前的残留上传文件**。

## 四、验证情况

- 语法与兼容：`node --check` 全部通过；无弯引号污染；无 Node 12 不支持语法；`启动.bat`（GB18030+CRLF）未改动。
- 后端 API（本地假 SMTP 真实走完 nodemailer 流程）：批次进度递增、取消（含取消项不落日志）、并发守卫、无 id 批次查询（刷新恢复）、拒发 index、CSV 下载（RFC 5987 中文文件名）、favicon 204 —— 全部通过。
- 前端 headless（Chrome 真内核）：第一轮 32/32、第二轮 12/12 断言全部通过，全程无页面 JS 错误。覆盖：手动添加弹窗、校验与警告、重复发送提醒、正文编辑（含实时预览）、整封预览、确认汇总、发送进度、已发送不重发、日志筛选/统计/导出、变量插入、总览条。

## 五、边界与说明

- 工具仍定位**本机/信任内网**使用，无登录口令（README 已有安全提醒）；上公网前必须加访问口令。
- 取消语义：为保证不重不漏，取消在"当前这封发完"后生效；未尝试条目不写日志、文件保留，可直接重发。
- 批次记录在内存中保留 30 分钟供查询，之后自动释放；发送结果以 CSV 日志为准。
