# 邮件批量发送工具 —— 交互设计规范与技术路线

> 用途：本文档供设计 review 与后续开发（含交给 Codex 等协作者做改动）参考。
> 项目代号 `complaint-mailer`，是一个**本地运行的 Windows 小工具**，用于按"类型 × 平台"批量发送投诉/监管类邮件。
> 最后更新：2026-08-03

---

## 一、项目定位

| 项 | 说明 |
|---|---|
| 用户 | 经办人员（非程序员），日常批量发监管件 / 先行引导 / 行内工单邮件 |
| 运行环境 | Windows 7 / 10 本地，**无公网服务、无数据库、无云端** |
| 形态 | 绿色便携包：双击 `启动.bat` → 后台起 Node 服务 → 自动打开 Edge 浏览器到 `http://localhost:3000` |
| 核心能力 | 上传一批文件 → 自动按文件名识别平台/客户/类型 → 核对 → 一键批量发送 |
| 关键约束 | 文件名即指令；非程序员友好；出错要一眼可见，绝不允许误发 |

---

## 二、交互与设计注意点

> 以下规范已在当前版本落地，后续改动**不得擅自削弱**这些体验。

### 2.1 信息架构（3 个 Tab）
1. **上传发送**（核心路径）：① 上传邮件正文/附件 → ② 核对分组 → ③ 确认发送
2. **模板管理**（旧称"类型管理"）：维护 3 类邮件的主题格式与正文模板（监管件 / 先行引导 / 行内工单）
3. **账号配置**：多 SMTP 发件账号管理（单启用互斥 + 连接测试）

> ⚠️ 旧的独立「发送」Tab 已删除，**唯一发送入口是「上传发送」**。

### 2.2 必须遵守的交互原则（来自 microinteractions / interaction-design）
- **就近反馈（Proximity）**：错误、状态必须出现在发生处的旁边，不要藏在 `title` tooltip 或需要横向滚动才看到的列里。
- **错误态醒目可见**：校验不通过时——分组卡片顶部红色横幅 + 错误行红色左竖线/浅红底 + 主行下方**全宽错误详情行** + 发送按钮禁用并附红字原因。四重可见，无法误发。
- **即时状态（Button feedback）**：任何可能耗时 >300ms 的操作（确认发送），点击后立即把按钮文案变「发送中…」并 disabled，完成/失败后再恢复，杜绝重复点击。
- **过渡动效克制**：shake / fade 等微交互控制在 200–400ms，仅用于强化"校验失败"等关键提醒，不做纯装饰。

### 2.3 关键注意点清单（按优先级）

**P0 — 不可妥协**
1. **发送闸门**：任一条目校验含错误（`errors` 非空），整批拒绝发送，发送按钮必须 disabled，且旁边显示红字「⚠ 有 N 封未通过校验，无法发送」。
2. **确认弹窗必须带汇总条**：顶部展示 `邮件总数 / 收件人数 / 附件数 / 平台数 / 发件账号`；存在未通过项时汇总条变红警告。让经办发送前一眼看清规模。
3. **错误文本全宽可见**：不要只放 `✗ 不通过` 小标，必须展开完整错误原因（如"平台『海尔』找不到收件邮箱"），不依赖 hover。
4. **连接测试绝不发信**：`/api/accounts/test` 只做 `transporter.verify()` 握手+认证，严禁触发真实发送。

**P1 — 体验关键**
5. **平台列宽度**：下拉 `min-width:130px`，确保「新浪国茂」「新浪航卓」完整显示，不截断。
6. **类型列可编辑**：移除顶部"邮件类型"选择栏后，**表格内"类型"列必须是下拉框**（选项含"默认：X" + 全部类型），识别错时用户能当场纠错，且切换后自动重新校验刷新主题/正文/收件。
7. **文件名解析边界**：文件名**必须含分隔符**（`-`/`_`/空格）；机构式命名 `海峡银行[类型]邮件-平台-客户` 的"邮件"前缀段须剔除，不能当平台/客户。
8. **正文 vs 附件分流**：`.md/.txt/.html/.htm` = 邮件正文；`.xlsx/.csv/.pdf/.doc/.png` 等 = 自动转该条邮件附件（正文 fallback 到类型模板）。两者是独立概念，表格只能当附件，不能当正文。
9. **拖拽跨平台**：每行左侧 ⠿ 可拖拽改所属平台分组，拖完后重置该校验态并重新预览。

**P2 — 一致性**
10. 后端不可达时，前端顶部显示「预览模式」横幅并禁用上传/校验/发送，避免用户以为能发。
11. 账号列表展示：名称 / Host / 账号 / 启用状态 / 操作（编辑·连接测试·删除），停用行提供「设为启用」按钮；编辑时密码留空 = 保留原密码。

### 2.4 反面清单（不要做）
- ❌ 把错误塞进 `title` 属性或右侧需滚动的列。
- ❌ 恢复"必须先点校验才回填"的逻辑（应在上传/改平台/改客户/改类型后**自动** debounce 预览回填）。
- ❌ 允许多个账号同时启用。
- ❌ 在便携包里打入 `data/`、`logs/`、`attachments/.uploads/` 等运行时目录（含账号明文密码）。
- ❌ 给非程序员暴露命令行参数或 JSON 配置编辑。

---

## 三、技术路线

### 3.1 技术选型与理由
| 技术 | 版本 | 用途 | 理由 |
|---|---|---|---|
| Node.js | 内嵌 **14.21.3**（`node14/node.exe`，便携） | 运行时 | 目标机 Win7/10 大概率无 Node；内嵌免安装 |
| Express | 4.22.2 | HTTP 服务 + 静态托管 | 轻量，单文件即可起服务 |
| nodemailer | 6.9.14 | SMTP 发信 | 事实标准，支持 verify 握手 |
| exceljs | 4.4.0 | 读写 `邮件配置.xlsx` | 平台/类型模板用 Excel 维护，非程序员友好 |
| markdown-it | 14.3.0 | 正文 MD→HTML | 模板正文为 Markdown |
| multer | 1.4.4 | 文件上传 | 接收正文/附件 |
| 前端 | **原生 JS 单页**（`public/index.html`，无框架/无打包） | UI | 改动直观、零构建、易审计 |

> 选型核心思想：**零构建、零数据库、零云端**。一切状态在内存 + 本地 Excel/JSON，双击即用。

### 3.2 目录结构
```
mailer/
├── 启动.bat              # Windows 启动器（设 MAILER_DATA_DIR，起服务，开 Edge）
├── start.sh              # Mac/Linux 启动器
├── server.js             # Express 入口：路由 + 静态托管 + API
├── send.js               # 旧 CLI 入口（保留，非主路径）
├── lib/
│   ├── accounts.js       # 多账号管理（单启用互斥、verify 测试、.env 迁移）
│   ├── draft.js          # compileJob：编译邮件作业（正文 fallback/附件/变量渲染）
│   └── ...
├── config/
│   └── 邮件配置.xlsx     # 类型模板（主题格式+正文MD）× 平台收件邮箱
├── data/                 # 运行时：accounts.json（密码明文，chmod 600）★不打入便携包
├── logs/                 # 运行日志 ★不打入便携包
├── attachments/.uploads/ # 上传临时文件 ★不打入便携包
├── public/index.html     # 前端 SPA（核心 UI）
└── node14/node.exe       # 内嵌便携 Node 运行时
```

### 3.3 后端模块
- **`server.js`**：`express.static('public')` 托管前端；端口 `PORT || 3000`，监听 `0.0.0.0`；关键 API：
  - `GET/POST /api/accounts`、`DELETE /api/accounts?id=`、`POST /api/accounts/test`（仅 verify）
  - `GET /api/state`：返回类型/平台配置 + 当前启用账号
  - `POST /api/preview-draft`：逐条 compileJob 回填收件/主题/正文 + 校验状态（**不发信**）
  - `POST /api/send-draft`：真正发送；无启用账号 → 友好报错且不发信
- **`lib/accounts.js`**：`data/accounts.json` 为数组，约束**同一时刻仅一个 `enabled=true`**（保存/启用时自动互斥）；首次启动自动从旧 `.env` 迁移首个账号；`getEnabledSmtp()` 返回当前启用账号或 null。
- **`lib/draft.js`**：`compileJob(job, {wb, uploadDir, attachDir})`——
  - 无上传正文或正文为空 → fallback 到类型模板 `正文MD` 并渲染变量
  - `readBody()` 检测二进制/null 字节返回空，避免 xlsx 读成 `PK…` 乱码
  - 变量语法单花括号 `{var}`：内置 `{日期}{日期中文}{星期}{时间戳}`，自动 `{平台名称}{客户姓名}`

### 3.4 前端架构
- 单文件 `public/index.html`：内联 `<style>` + `<script>`，原生 DOM 操作，无外部 CDN（离线可用）。
- 状态对象 `UP`：`{ entries:[], type, contact, ... }`，每个 entry 含 `platform/customer/type/bodyFileId/attachments/_status/_errs/_preview`。
- 关键函数：`parseFileName()`、`renderUpload()`、`debouncedPreview()`、`renderConfirmModal()`、`jobsFromEntries()`。
- 渲染后 `bindUploadEvents()` 绑定下拉/输入/拖拽/删除事件；改动平台/客户/类型会重置该校验态并触发 `debouncedPreview`。

### 3.5 数据流（上传发送主路径）
```
用户上传文件
  → 前端按扩展名分流（文本=正文 / 其它=附件）
  → parseFileName 解析 平台/客户/类型（文件名即指令）
  → 按平台分组为可编辑表格（拖拽跨组、改平台/客户/类型、追附件）
  → debouncedPreview 自动调 /api/preview-draft（compileJob 回填 收件/主题/正文 + 校验）
  → 用户点「校验」强制刷新（有错→横幅+行高亮+全宽错误详情，禁用发送）
  → 点「确认发送」→ renderConfirmModal 汇总条 → 确认 → /api/send-draft
```

### 3.6 文件名解析算法（`parseFileName`）
支持两种命名，统一归一化：
1. **简式**：`平台-客户[-类型]`（顺序无关，如 `新浪国茂-张三`）
2. **机构式**：`海峡银行[类型]邮件-平台-客户`（如 `海峡银行监管件邮件-新浪国茂-测试客户甲`）
3. **带前导类型标签**：`监管件：海峡银行监管件邮件-对接平台-投诉客户姓名.xlsx`（全角冒号前缀，`对接平台`/`投诉客户姓名` 为待替换占位符）

关键逻辑：
- `isHeader`：同时含类型关键字 + "邮件"的首段（如`海峡银行监管件邮件`）自动剔除，不当平台/客户。
- `detectTypeName`：**优先只扫标题前缀段**，避免客户名含"引导/工单/监管"干扰（曾把`行内客服工单`误判成`先行引导`）。
- `normalizePlatform`：精确匹配 → 段内含平台名(≥2字) → 别名表双向互认（`国茂↔新浪国茂`、`航卓↔新浪航卓`）。
- `TYPE_KEYWORDS`：`监管件` / `先行引导`(先行/引导) / `行内工单`(行内客服工单/行内客户工单/工单)。
- 客户 = 剔除平台段 + 类型关键字段后用空格拼接；多客户（、/，分隔）整体保留为一封。

### 3.7 多账号与发信
- 多个 SMTP 账号可保存，但**全局仅一个启用**；启用新账号自动停用旧启用账号。
- 发送一律走 `accounts.getEnabledSmtp()`；无启用账号 → 拦截并报「尚未启用任何发件账号」。
- `连接测试` 仅 `transporter.verify()`，不发信、不落库。

### 3.8 部署与打包（Windows 便携）
- **打包脚本** `build_zip.py`（Python `zipfile`）：遍历 `mailer/` 写入 zip，**排除** `mailer/data`、`mailer/logs`、`mailer/attachments/.uploads`（带 `mailer/` 前缀精确匹配）；zipfile 默认 UTF-8，中文文件名/路径正常。
- **启动器** `启动.bat`：`cd /d "%~dp0"` → `set MAILER_DATA_DIR=%~dp0data`（不存在则建）→ 优先用内嵌 `node14\node.exe` 否则回退系统 `node` → `start` 后台跑 `server.js` → 2 秒后打开 Edge 到 `localhost:3000`。
- 产物：`邮件批量发送工具-便携包.zip`（约 3200+ 条目，含内嵌 node.exe；运行数据不入库）。

### 3.9 已知限制 / 坑
- **`海尔` 等用户示例平台**：若 `邮件配置.xlsx` 未配置其收件邮箱，解析能建分组但校验会报"找不到收件邮箱"——属正常，需用户在「模板管理/平台」补邮箱。
- **多客户（如`甲、乙、丙`）**：当前作为单个客户字段/单封邮件处理，拆分多封是后续可选项。
- **账号密码明文**存 `data/accounts.json`（chmod 600），便携包已排除该目录，但本地文件仍明文，勿外传 data/。
- 改前端后必须用 `node --check` 校验抽取的 `<script>`，并用无头浏览器（puppeteer-core + 本机 Chrome，`--no-sandbox`）跑"正常路径 + 错误路径"两条断言再重建 zip。

---

## 四、给 Codex / 后续改动者的建议
1. **改动前先起服务 + 跑无头验证**（见 3.9 末尾），不要只靠肉眼读代码。
2. **守住发送闸门与错误可见性**（2.3 P0）——这是工具的法律/业务安全底线。
3. 任何涉及"文件名解析"的改动，必须用机构式 + 简式 + 带标签三种命名各测一遍（见 3.6）。
4. 打包前确认 `build_zip.py` 的排除项带 `mailer/` 前缀，否则账号明文会泄露进 zip。
5. 不要引入需要 `npm install` 或构建步骤的依赖；目标机离线、无网络。
